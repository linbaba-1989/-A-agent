# xtquant_230825b isolated probe

Source: `https://dict.thinktrader.net/packages/xtquant_0825b_2023-09-20.rar`

The archive is linked by the official ThinkTrader knowledge-base download page as the broker-compatible public build. This directory is never added to the production application's `sys.path`; `probe.py` must run as a separate process.
